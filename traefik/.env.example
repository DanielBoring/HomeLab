# Traefik Ports
HTTP_PORT=80
HTTPS_PORT=443

# Timezone (e.g. America/New_York, Europe/London, UTC)
TZ=UTC

# Dashboard domain — set to your local hostname or domain
# Access the dashboard at http://<TRAEFIK_DOMAIN>/dashboard/
TRAEFIK_DOMAIN=traefik.localhost
