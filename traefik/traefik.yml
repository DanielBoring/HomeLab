api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
    network: traefik

log:
  level: INFO

# Uncomment and configure to enable Let's Encrypt TLS certificates
# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: "your-email@example.com"
#       storage: /letsencrypt/acme.json
#       httpChallenge:
#         entryPoint: web
