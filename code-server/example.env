TZ=America/New_York
PUID=3001
PGID=3001

CODESERVER_DOMAIN=code.yourdomain.com

# Web UI password (plaintext). Use HASHED_PASSWORD instead for better security.
CODESERVER_PASSWORD=changeme

# Optional: sudo password for the terminal inside code-server
CODESERVER_SUDO_PASSWORD=changeme
