TZ=America/New_York

# Web UI domain (also used as the DNS server's own hostname in SOA records)
TECHNITIUM_DOMAIN=dns.virtuallyboring.com

# Admin panel password — change before first boot
TECHNITIUM_ADMIN_PASSWORD=changeme
